// Deprecated duplicate ThemeProvider. Keeping stub for backward compatibility.
// Prefer using `contexts/ThemeContext` exports.
import React from 'react';

const ThemeProvider = ({ children }) => children;

export default ThemeProvider;
